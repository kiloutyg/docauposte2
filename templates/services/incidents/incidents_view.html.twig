{% extends 'base.html.twig' %}
{% block title %}
	{{incident.name|split('.')|first|capitalize}}
	|
	{{ parent() }}
{% endblock %}
{% block body %}
	<h1>{{incident.name|split('.')|first|capitalize}}</h1>
	<iframe class="h-100" src="{{ path('app_incident_download_file', { 'name': incident.name }) }}" style="width:100%;" target="_blank"></iframe>

	<h1>{{incident.name|split('.')|first|capitalize}}</h1>
	<iframe class="inner-container d-flex flex-grow-1 flex-column h-100 position-relative" src="{{ path('app_incident_download_file', { 'name': incident.name }) }}" width="100%" height="100%" target="_blank"></iframe>
	<iframe class="inner-container d-flex flex-grow-1 flex-column h-100 position-relative" src="{{ path('app_incident_download_file', { 'name': incident.name }) }}" width="100%" height="100%" target="_blank"></iframe>

{% endblock %}
